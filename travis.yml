language: php

php:
  - '7.4snapshot'
  - nightly

matrix:
  fast_finish: true

cache:
  directories:
    - $HOME/.composer/cache

before_script:
  - travis_retry composer install -n

script:
  - vendor/bin/phpunit
