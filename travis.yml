language: php

matrix:
  fast_finish: true
  include:
    - php: 7.4snapshot
    - php: nightly

cache:
  directories:
    - $HOME/.composer/cache

before_script:
  - composer install -n

script:
  - vendor/bin/phpunit

sudo: false
